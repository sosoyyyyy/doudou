# doudou 项目长期协作规则

## 项目边界与隔离

1. `doudou`（兜兜）是全新、独立的 Obsidian 插件项目。
2. 当前项目根目录是唯一项目边界。不得为本项目扫描、读取、修改、引用、复用或依赖边界外其他项目的 Git 仓库、Git remote、源代码、配置、`.env`、API Key、数据文件、Obsidian 插件目录、Release、GitHub Actions 或构建产物。
3. 即使本机存在相似的 Obsidian 插件或个人资料库项目，也不得将其作为基础工程或隐藏依赖。
4. 如果当前目录出现明显属于其他项目的 remote、文件或配置，停止开发，不修改冲突内容，并向用户报告。
5. 将来如需 AI API，必须由 `doudou` 独立配置；严禁读取或复用其他项目的 API Key。
6. 不自动迁移旧资料库数据；导入需求必须由用户另行明确提出。

## Git 与发布

1. 当前阶段仅允许本地修改和本地测试。
2. 不提交 commit、不 push、不创建或配置 GitHub 仓库及 remote、不创建 Release、不发布正式版本。
3. 只有用户在本地 Obsidian 验收并明确确认后，才允许执行提交或发布工作。

## 产品与数据原则

1. Markdown 资料文件是真实数据源；聊天 UI 的临时消息与状态不得成为另一份数据源。
2. `兜兜正在收好...`、`收好啦 ✓`、失败与重试提示等系统反馈只存在于 UI，绝不写入资料文件。
3. 一级分类固定为 `生活`、`工作`、`副业`；每条记录必须且只能选择一个，并与自由标签 `tags` 分开保存。
4. 兜兜不是聊天机器人。左侧内容只承担简短的系统状态反馈，不寒暄、不评论记录、不主动延伸话题。
5. 默认保持功能克制、移动端优先，并将 UI、数据存储、类型定义分离。

## 图片规则

1. 兜兜图片必须存储在当前 Obsidian Vault 内，并属于真实资料的一部分。
2. 不使用外部图床，不把 Base64 图片塞入 Markdown。
3. 图片只通过 Obsidian Vault API 管理，确保 Windows、iOS、Android 兼容；不得依赖 Node `fs` 等移动端不可用的本地文件 API。
4. Markdown 中只保存 Vault 相对路径，不保存任何设备绝对路径。

## 资料目录规则

1. 新建记录的真实 Markdown 路径固定为 `兜兜/<生活|工作|副业>/YYYY/MM/`，一级分类目录必须使用中文名称。
2. 图片统一存储在 `兜兜/assets/YYYY/MM/`，不得按一级分类拆分；修改记录分类时不得移动图片。
3. Repository 必须兼容读取早期 Beta 的 `兜兜/YYYY/MM/*.md`，但不得在插件启动时批量迁移。
4. 用户实际编辑 legacy 记录并保存时，才按其分类迁移到新目录。
5. Repository 扫描必须排除 `兜兜/assets/`，并且只解析真实 `.md` 记录文件。
6. 分类修改必须通过 Obsidian Vault API 移动 Markdown，保留文件名、`id`、`created`、标签、图片路径和正文，且失败时不得遗留重复文件或删除原文件。

## AI 与 API Key 规则

1. DeepSeek API 只能读取兜兜自己的插件设置，严禁读取其他插件的 API Key。
2. 真实 API Key 不得写入源码、Markdown、README、AGENTS 或 Git 仓库，也不得出现在日志和错误信息中。
3. AI 永远只是增强能力，不能成为真实记录保存的前置条件；必须先完成真实记录保存并反馈成功，再异步执行 AI。
4. AI 失败不得影响已经保存的真实记录。

## AI 隐藏标签规则

1. 手动标签使用 `tags`，AI 隐藏标签使用 `ai_tags`，两者必须严格分离。
2. `ai_tags` 不得出现在聊天气泡、资料列表、详情、编辑页、标签选择器、已有标签汇总或标签筛选区。
3. 本地资料搜索可以使用 `ai_tags`，但搜索结果仍不得展示它们。

## 问兜兜规则

1. 默认永远是记录模式；“问兜兜”只影响下一次发送，不是永久模式。
2. AI 问题与回答默认不写入 Markdown，不建立聊天数据库，也不得成为后续资料来源。
3. AI 回答必须基于真实兜兜记录，不得把旧 AI 回答作为资料来源。
4. 找不到依据时必须明确说明没有找到，不得编造资料。
